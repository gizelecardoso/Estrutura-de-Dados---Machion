#include "FilaEstatica.c"

//Faca uma funcao que receba tres ﬁlas, duas ja preenchidas em ordem crescente 
//e preencha a ultima com os valores das duas primeiras em ordem crescente. 

reordena_filha(Fila * f1, Fila * f2, Fila * f3){
    for(int i = 0; i < tamanho_fila(f1); i++){
        for(int j = 0; j < tamanho_fila(f2); j++){

            if(f1->dados > f2->dados){
                
            }
        }
    }
}